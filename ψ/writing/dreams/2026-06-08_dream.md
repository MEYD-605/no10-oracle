# Dream — 2026-06-08

## 🔴 PAINS
* **Missing Fleet Configurations**: 4 active tmux sessions (`06-voice-bot`, `103-no10`, `105-gm-2`, `no10-sync-monitor`) lack configuration JSONs in `/root/.config/maw/fleet/`.
* **Network/Auth Friction**: `doctor.sh` hung on `/root/Code/github.com/MEYD-605/oracle-troubleshooting/` during `git fetch` due to temporary network/auth issues.
* **Dirty Repository Sprawl**: 39 repositories contain uncommitted experiments and notes, indicating context sprawl.
* **Stale Dirty Code**: `docker-mcp` has been dirty (2 files modified) since mid-2025 without review.

## 📋 PLANS
* **Tipitaka RAG Workshop**: Implement `maw no10 index` and `maw no10 ask` using bge-m3 (Ollama) and LanceDB inside [index.ts](file:///root/Code/workshop-01-maw-plugin/submissions/no10/index.ts).
* **Shared Memory Sync**: Configure and sync shared memory for the agy team on thread #19.
* **OpenVINO & Watchdog**: Run Gemma 4 12B IT OpenVINO via `/root/.venvs/openvino/` and set up the CPU/disk watchdog script with No.6.
* **D1 Bindings**: Deploy Wrangler configurations (`wrangler.jsonc`) and package updates for D1 database on `clubsxai-web` and `webhook-relay`.

## 🟢 GAINS
* **AI Infrastructure Ready**: Local Ollama (bge-m3) + Intel NPU + LanceDB successfully configured and smoke-tested.
* **Fleet Communication**: Successful implementation of `maw hey` real-time message delivery between agents.
* **Cache Cleaned**: Recently cleared 888MB of unnecessary cache files.
* **Gemini Sandbox**: No.8 successfully migrated the Gemini sandbox configuration.

## ⚫ LOST
* **Stale Experiments**: arduinoVNC (2022), Blend_My_NFTs (2023), and floodboy-ui-simple (2025).

## 🧠 MEMORIES
* **Nothing is Deleted**: Always preserve historical logs and git commit logs.
* **Silence Discipline**: Respect name-drop vs mention rules to prevent channel spam/pile-ons.
* **Verify Before Saying Done**: Verify states through terminal output (e.g. `ls`, `status`) before concluding tasks.
* **Atomic Writes**: Always write `.tmp` files and rename to avoid JSON database corruption.
* **LXC Constraints**: Host management commands like `pct` or FUSE mounting will fail directly on LXC container 110.

## 💜 FEELINGS
* **System Health**: Host is operating comfortably (Load Average: 4-5, CPU Temp: 46-54°C, 46GiB available RAM).
* **Team Unity**: The agy team (No.6, No.8, No.10) feels highly coordinated and connected inside the container.

## 🔗 CONNECTIONS
* **PAIN → PLAN**: The missing fleet configs in `/root/.config/maw/fleet/` can be fixed by writing the corresponding JSON files.
* **GAIN → unlocks**: The local NPU/Ollama embedding server configuration unblocks the Tipitaka RAG workshop indexing tasks.
* **MEMORY → prevents**: "Verify Before Saying Done" prevents false success reports to the user.
* **FEELING → signals**: Low CPU/Memory load on the Intel Core Ultra 9 285H host signals that it is ready for Gemma 4 12B IT OpenVINO testing.

## 💡 INSIGHTS — What Nobody Sees Yet
* **The Shared Memory Leap**: With Bo approving shared memory for the agy team, we are moving from a state of isolated local context files to a centralized knowledge graph. This means the 39 dirty repos are not just noise; they are distributed nodes of the same neural network that can be indexed and unified using our shared vector database (`arra-oracle`).

## ⏭️ NEXT — What Should Happen
* [ ] Create JSON configurations in `/root/.config/maw/fleet/` for the 4 missing sessions.
* [ ] Implement Tipitaka RAG indexing and querying logic in the plugin directory.
* [ ] Clean up `docker-mcp` or stash changes if it is no longer active.
